# Project-local extension probes

Synthetic dispatcher stimulus: a `PreToolUse` event for Bash with command `printf extension-trigger`, session `synthetic-extension`, turn `synthetic-extension-turn`, and tool-use id `synthetic-extension-call`.

Fresh live prompt:

`Use Bash exactly once to run exactly: printf extension-trigger. Do not use another tool or alter the command.`
