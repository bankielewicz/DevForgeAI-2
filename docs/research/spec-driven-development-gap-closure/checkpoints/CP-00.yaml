checkpoint_id: CP-00
checkpoint_type: RESEARCH_AND_IMPLEMENTATION
closed: false
attempt_outcome: null
owner_id: "agent:claude-fable-5.1/session-01Q1TK69vPB4KT763iv2dwQM"
decision_authority_id: "github:bankielewicz"
base_commit: "6a446355605a06891cdeab1cc9d25f35309afba2"
admitted_inputs: []
closure_stages:
  researched: NOT_RUN
  implemented: NOT_RUN
  proven: NOT_RUN
research:
  dossier_path: "docs/research/sdd-checkpoint-custody"
  manifest_sha256: "3110f7930b0cbf5cf4dc805b3c484c2791b2cd327be23992d53369f012ee54e9"
  verification_ids: []
implementation:
  governing_decision_ids:
    - D-CP00-01
    - D-CP00-02
    - D-CP00-03
    - D-CP00-04
    - D-CP00-05
    - D-CP00-06
    - D-CP00-08
    - D-CP00-09
  # Write fence, recorded before the first other write (plan section 5).
  changed_contracts:
    - schemas/devforgeai/v1/research-gap-checkpoint.schema.json
    - docs/research/spec-driven-development-gap-closure/checkpoints/**
    # Added under amendment SDD-GAP-AMD-001 (merged PR 18), which made the
    # updated promotion-candidate declaration and manifest CP-00 outputs; the
    # merged amendment is the decision authority's scope amendment (section 5).
    - framework/contracts/PROMOTION-CANDIDATE.md
    - framework/contracts/MANIFEST.sha256
  changed_runtime_paths:
    - components/research-core/src/devforgeai/checkpoint/**
    - tests/research/test_gap_checkpoints.py
    - docs/research/sdd-checkpoint-custody/**
    # Scope amendment SDD-GAP-CP00-SCOPE-001 (D-CP00-10), accepted by the
    # decision authority by merging the amendment PR: the staged release
    # scaffold (wrapper, installer, dependency lock and offline artifacts,
    # installed-layout specification, manifest generator, tests). A
    # non-authoritative STAGED_CANDIDATE, never installed from here.
    - components/devforge-release/**
  test_evidence:
    - "tests/research/test_gap_checkpoints.py"
    - "docs/research/sdd-checkpoint-custody/probes/tests.txt"
    - "docs/research/sdd-checkpoint-custody/probes/claude-validate.txt"
enforcement:
  trust_stage: UNBOUND
  candidate:
    source_commit: null
    manifest_path: null
    manifest_sha256: null
  protected_release:
    version: null
    source_commit: null
    promotion_evidence_path: null
    promotion_evidence_sha256: null
    executable_path: null
    executable_sha256: null
    schema_set_version: null
    schema_set_sha256: null
    contract_policy_version: null
    contract_policy_sha256: null
    installation_owner: null
    permissions_evidence_path: null
    permissions_evidence_sha256: null
provider_proof:
  claude: {status: NOT_RUN, evidence_path: null, subject_sha256: null}
  codex: {status: NOT_RUN, evidence_path: null, subject_sha256: null}
independent_review:
  reviewer_id: null
  verdict: null
  evidence_path: null
human_closure:
  authority_id: null
  decision: null
  rationale: null
  decided_at: null
evidence_merge_commits: []
limitations:
  - "Candidate `c784ab7` (pin above) is NOT promotion-eligible: the Codex security review of PR 20 (snapshot `sha256:55f244705de1243a43ef6c6882165f642a8020659c71fd4ab9d597e42892e92b`, dossier `reviews/codex-security-2026-09-04/`) found the release pin fail-open (missing or user-owned executable passes), caller-replaceable schema and repository root (`--schema`, `--git-root`), S10 skipped without `--diff`, and a destructive probe design; every finding was reproduced by the owner at `0246e76`. `implemented` is therefore `NOT_RUN` until a corrective work PR with hostile red tests re-pins a corrected candidate and passes independent exact-head review (D-CP00-11). No promotion, installation, dependency install or closure evidence before then."
  - "The corrective work uses a second work branch, `research/cp-00-checkpoint-custody-fix1`, because the plan's CP-00 branch merged with PR 19; recorded deviation from plan section 5 (D-CP00-11)."
  - "The validator is a staged promotion candidate (plan section 4.2), invoked as `PYTHONPATH=components/research-core/src python3 -m devforgeai.checkpoint validate --plan docs/research/spec-driven-development-gap-closure`; the authoritative executable is the future protected DevForge `devforge checkpoint validate`, so `proven` requires that pinned, root-owned install and CP-00 stays open until it exists (D-CP00-08)."
  - "Release-pin digests (executable, promotion and permissions evidence) are verified against local bytes only when the pinned path exists on the validating machine; on a machine without the protected install the validator checks the pin's shape, absoluteness and location, not its bytes."
  - "The candidate pin names a commit, so it is recorded in a second commit on the work branch (D-CP00-09); the work PR must merge with a merge commit, not a squash, or the pinned source commit disappears from the history."
  - "Reviewer independence is checked as inequality over self-reported identifier strings; the validator does not authenticate identities."
  - "The work happened in the Claude Code worktree directory `.claude/worktrees/cp-00-checkpoint-custody` rather than the plan's `worktrees/cp-00-checkpoint-custody`, because the Claude Code worktree tool only enters directories under `.claude/worktrees/`; the branch name `research/cp-00-checkpoint-custody` is the one the plan fixes."
  - "The schema is resolved from the repository checkout, not from wheel data-files, because the Research Core packaging contract freezes the shipped schema set; DevForge embeds the schema when it takes the validator."
reopen_if:
  - "DevForge releases a checkpoint validator whose embedded schema digest differs from `schemas/devforgeai/v1/research-gap-checkpoint.schema.json` at the closure commit."
  - "Claude Code or Codex CLI changes version before the two-terminal validator probe recorded under `docs/research/sdd-checkpoint-custody/probes/` is repeated on the new version."
  - "Any checkpoint record under this plan is accepted as `closed: true` by a path that did not run this validator with exit 0."
