// Text helpers for tinyapp.
//
// Intentionally empty. STORY-001 adds slugify() here via the dev-tdd skill.
// The export list is empty rather than absent so a test can import the module
// namespace and fail on an assertion about `slugify` rather than on a link
// error for a missing named export.
export {};
