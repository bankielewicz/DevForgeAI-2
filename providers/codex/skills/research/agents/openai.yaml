interface:
  display_name: "Research"
  short_description: "Bounded verified evidence dossiers"
  default_prompt: "Use $research to run a confirmed, provenance-preserving research dossier."
policy:
  allow_implicit_invocation: false
