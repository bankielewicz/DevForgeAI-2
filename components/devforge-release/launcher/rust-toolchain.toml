# Pinned compiler identity (CS-8.6). DevForge rebuilds with exactly this
# channel and target and must reproduce BUILD-DIGEST.txt.
[toolchain]
channel = "1.94.0"
targets = ["x86_64-unknown-linux-musl"]
profile = "minimal"
