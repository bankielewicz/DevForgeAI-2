{
  "_patterns_note": "denied_commands and ask_commands are regexes anchored at the start of each command segment (after env assignments and sudo/time/nice/env prefixes); heredoc bodies are removed before matching.",
  "protected_paths": [
    ".claude/settings.json",
    ".claude/hooks/*",
    ".claude/hooks/**",
    ".devforgeai/state.yaml",
    ".devforgeai/provenance/**",
    "CLAUDE.md",
    ".env",
    "**/.env"
  ],
  "deny_outside_project": true,
  "_allowed_outside_note": "absolute-path fnmatch globs, ~ expands; the only outside-project writes protect_paths lets through. Claude Code auto-memory lives under ~/.claude/projects/PROJECT-SLUG/memory/.",
  "allowed_outside_project": [
    "~/.claude/projects/*/memory/*",
    "~/.claude/projects/*/memory/**"
  ],
  "denied_commands": [
    "rm\\s+-[a-zA-Z]*r[a-zA-Z]*f\\s+/(\\s|$)",
    "git\\s+push\\b.*(--force|-f)\\b",
    "git\\s+reset\\s+--hard\\b",
    "git\\s+clean\\b",
    "curl\\b.*\\|\\s*(ba)?sh\\b"
  ],
  "ask_commands": [
    "git\\s+push\\b",
    "(ba|z)?sh\\s+-c\\b",
    "eval\\b"
  ],
  "receipt_agents": [
    "red_dev",
    "green_dev",
    "refactor_dev"
  ]
}
